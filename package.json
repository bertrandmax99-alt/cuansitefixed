{
  "name": "cuansite",
  "version": "1.0.0",
  "description": "CuanSite — Web Design Agency",
  "main": "server.js",
  "scripts": {
    "build": "mkdir -p dist && cp index.html admin.html dist/ && cp -r uploads dist/uploads 2>/dev/null || true",
    "lint": "echo 'No lint step'",
    "test": "node -e \"require('./server.js')\"",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cookie-session": "^2.1.1",
    "express": "^4.21.0",
    "firebase": "^12.12.1",
    "jimp": "^1.6.1",
    "jsdom": "^29.0.2",
    "multer": "^1.4.5-lts.1",
    "netlify-cli": "^25.0.0",
    "serverless-http": "^4.0.0"
  },
  "overrides": {
    "fetch-blob": "3.1.2"
  }
}
